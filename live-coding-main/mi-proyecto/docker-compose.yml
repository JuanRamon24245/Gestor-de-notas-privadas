version: '3.8'
services:
  app:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend/db.json:/app/db.json
    environment:
      - PORT=3000
      - JWT_SECRET=cambia_esto_en_produccion
    restart: unless-stopped